[project]
name = "aoaa-atmospheric-optics-academic-analyzer"
version = "3.3.0"
description = "A Streamlit workbench that analyzes academic literature through atmospheric optics metaphors."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
